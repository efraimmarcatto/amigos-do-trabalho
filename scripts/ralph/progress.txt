# Ralph Progress Log
Started: Thu Mar 12 21:45:43 PDT 2026
---

## Codebase Patterns
- GlobalInput is a C++ GDExtension singleton registered via Engine::register_singleton in register_types.cpp — no autoload needed
- The .gitconfig has gpg.format that must be set to "openpgp" (was empty/broken)
- Project uses Godot 4.6 with GL Compatibility renderer and transparent viewport
- Save data stored at user://save_data.json — extend this for future features
- Pet visual states (SAD/NEUTRAL/HAPPY) use modulate color tinting on the Sprite2D — now called PetMood (separate from PetState movement enum)
- pet.gd uses PetState enum (IDLE, WALKING, FALLING, DRAGGED, INTERACTING) for movement; _process() delegates to _process_<state>() handlers
- main.gd is a Control node that manages passthrough polygon for click-through transparency
- _update_passthrough() collects Rect2 from all visible elements and merges them — add new UI elements there
- _change_state() in pet.gd handles enter/exit visual effects for state transitions — add new state visuals there
- Floor Y for pet landing = DisplayServer.screen_get_size().y - (texture height * scale.y) / 2.0
- Window is set to fullscreen at runtime via DisplayServer.screen_get_size() in main.gd _ready()

---

## 2026-03-12 - US-001
- Replaced mock GlobalInput autoload with real GDExtension C++ singleton
- Removed [autoload] section from project.godot (GlobalInput registers itself via Engine::register_singleton)
- Added GlobalInput.start_hooks() call in main.gd _ready()
- Added GlobalInput.stop_hooks() call in main.gd _notification(NOTIFICATION_WM_CLOSE_REQUEST) before quit
- Deleted scripts/mock_global_input.gd
- Files changed: project.godot, scripts/main.gd, scripts/mock_global_input.gd (deleted)
- **Learnings for future iterations:**
  - The GDExtension singleton auto-registers via register_types.cpp — just removing the autoload is sufficient
  - coin_system.gd references GlobalInput.get_key_count(), get_click_count(), reset_counts() which resolve to the C++ singleton
  - The .gitconfig had a broken empty gpg.format value that needed fixing to "openpgp"
  - global_input.gdextension file at project root maps to compiled .so/.dll in gdextension/bin/
---

## 2026-03-12 - US-002
- Set window to fullscreen transparent: DisplayServer.screen_get_size() + position Vector2i.ZERO in main.gd _ready()
- Updated project.godot viewport to 1920x1080 defaults with window overrides
- Refactored _update_passthrough() to dynamically merge rects from all visible elements (pet, coin label, interaction menu)
- Repositioned pet sprite, coin label, and interaction menu for fullscreen layout
- Files changed: scripts/main.gd, project.godot, scenes/main.tscn
- **Learnings for future iterations:**
  - Window sizing at runtime uses DisplayServer.screen_get_size() and get_window().size/position
  - _update_passthrough() uses Rect2.merge() to combine multiple element rects into one bounding polygon
  - project.godot window_width_override/window_height_override set the actual window size separately from viewport
  - No Godot binary available in CI — typecheck must be verified manually or in a Godot-enabled environment
---

## 2026-03-12 - US-003
- Added PetState movement enum (IDLE, WALKING, FALLING, DRAGGED, INTERACTING) to pet.gd
- Renamed old PetState (mood) enum to PetMood to separate visual mood from movement state
- Added _process() with state dispatch to _process_idle(), _process_walking(), _process_falling(), _process_dragged(), _process_interacting()
- Added _change_state() helper method for state transitions
- Updated main.gd save to use _current_mood instead of _current_state
- Files changed: scripts/pet.gd, scripts/main.gd
- **Learnings for future iterations:**
  - PetMood (SAD/NEUTRAL/HAPPY) is independent of PetState (IDLE/WALKING/etc.) — mood is coin-driven, state is movement-driven
  - main.gd _save_state() references pet_sprite._current_mood — keep in sync if renaming
  - _load_state() doesn't actually restore mood (only coins), so the save key rename is safe
  - State handler methods are placeholders — US-004/005/011 will fill them in
---

## 2026-03-12 - US-004
- Implemented pet drag-and-drop with gravity physics
- Added @export var gravity: float = 980.0 for configurable gravity
- Added drag tracking with 4px threshold to distinguish click from drag
- _process_dragged() follows cursor; _process_falling() applies gravity with velocity integration
- Pet lands on screen bottom (screen_height - half sprite height) and transitions to IDLE
- Grabbed visual: 5° rotation while dragged, reset on release
- _input() rewritten to handle press/release/motion for drag detection
- Menu only opens on click (no drag), drag hides menu if open
- Files changed: scripts/pet.gd
- **Learnings for future iterations:**
  - _change_state() now handles enter/exit visual effects — add new state visuals there
  - Floor Y calculation: DisplayServer.screen_get_size().y - (texture.get_size().y * scale.y) / 2.0
  - _velocity is reset to Vector2.ZERO when entering FALLING from drag — pet falls from rest
  - When dragging interrupts other states, ensure _mouse_pressed tracking stays consistent
  - US-005 (walking) and US-010 (furniture surfaces) will need to update the floor detection logic
---
