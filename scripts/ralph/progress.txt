# Ralph Progress Log
## Codebase Patterns
- Keep edit-mode affordances on furniture in `scripts/main.gd` as runtime-only child controls and resync them from `_edit_mode` whenever furniture is spawned or restored.

Started: Sat Mar 21 21:19:34 PDT 2026
---
## 2026-03-22 04:28:24 UTC - US-001
- Implemented a shared edit-mode sync path so furniture highlights and `RemoveButton` controls are added only while layout edit mode is active, with the same logic reused when furniture is spawned during runtime.
- Files changed: `scripts/main.gd`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: furniture instances are spawned dynamically under `FurnitureContainer`, so edit-mode UI must be synchronized in code rather than assumed from scene state.
  - Gotchas encountered: this environment does not provide a `godot` binary, so headless typecheck/runtime validation could not be executed here.
  - Useful context: browser/manual UI verification is still needed for layout edit behavior because no browser tooling is configured in this workspace.
---
