# Ralph Progress Log
Started: Fri Mar 13 14:59:40 PDT 2026
---

## Codebase Patterns
- UI panels (shop, coin HUD, etc.) are positioned programmatically in `setup()` using `floor_y` and `DisplayServer.screen_get_size()`
- All interactive UI must be included in the passthrough polygon via `_update_passthrough()` in main.gd — use `get_rect()` or `get_global_rect()` helpers
- Tween animations use 0.3s duration, `EASE_OUT`, `TRANS_CUBIC` — match this for consistency
- slide_menu.gd emits `menu_opened`/`menu_closed` signals for coordinating UI animations
- Adjacent panels (shop, inventory, settings) use `setup(menu_open_x, menu_panel_y)` for positioning relative to the slide menu
- Use `on_before_close: Callable` on slide_menu to intercept close and sequence panel close animations
- Panel open/close uses tween on `position:x` with `_animate_x()` pattern — hidden off-screen at `screen_w`, visible at computed `_open_x`
- When adding new adjacent panels, update these locations in main.gd: `_handle_menu_close()`, `_update_passthrough()`, `_on_edit_layout_requested()`, `_enter_placement_mode()`
- coin_hud.gd builds its UI programmatically in `_build_ui()` (no child nodes in .tscn)
- The CoinSystem emits `coins_changed(new_total: int)` — connect to this for coin display updates
- InventorySystem follows the same pattern as CoinSystem: extends Node, emits `inventory_changed(furniture_id, new_count)` signal
- JSON parses numbers as float — always handle both `is float` and `is int` when loading numeric values from save_data.json
- Inventory stored as `Dictionary[String, int]` mapping furniture_id to quantity; items with 0 count are erased from the dictionary

---

## 2026-03-13 - US-020
- Replaced CoinLabel (plain text "Coins: 0") with styled CoinHud (PanelContainer with coin icon + animated counter)
- CoinHud: white/light background, rounded corners (StyleBoxFlat), yellow circle icon placeholder, dark text
- Positioned directly left of the menu toggle button at bottom-right
- Counter animates on value change using Tween (~0.3s interpolation)
- Coin HUD moves upward when slide menu opens, returns to base position on close (same tween timing)
- Included in mouse passthrough polygon via `get_rect()` helper
- Added `menu_opened`/`menu_closed` signals to slide_menu.gd
- Files changed: scripts/coin_hud.gd (new), scripts/main.gd, scripts/slide_menu.gd, scenes/main.tscn
- **Learnings for future iterations:**
  - The scene tree expects `$CoinHud` — if renaming, update both main.gd `@onready` and main.tscn node name
  - `coin_hud.setup()` uses `await get_tree().process_frame` to wait for layout — call it after adding to tree
  - No Godot binary available in this environment for typecheck — manual code review required
  - ext_resource in .tscn needs a uid for Godot to auto-generate; omitting uid works but Godot will assign one on next editor open
---

## 2026-03-13 - US-021
- Shop panel now slides in/out adjacent to (left of) the slide menu with tween animation
- Shop positioned at `menu_open_x - SHOP_WIDTH - GAP` via new `setup()` method
- 0.1s delay before shop open animation for chained visual effect
- Close sequencing: if shop is open when menu closes, shop closes first (0.3s), then menu closes
- `on_before_close` callable on slide_menu intercepts all close paths (toggle, click-outside)
- Shop toggle behavior: clicking Shop button again closes the shop
- Removed shop's own `_unhandled_input` close — all close coordination through main.gd
- Shop close on edit layout and placement mode entry
- Shop included in passthrough polygon via `is_shop_open()` check
- Files changed: scripts/shop.gd, scripts/slide_menu.gd, scripts/main.gd
- **Learnings for future iterations:**
  - `_get_panel_open_x()` renamed to `get_panel_open_x()` (public) — use this for adjacent panel positioning
  - `get_panel_y()` added to slide_menu for aligning panels vertically
  - When adding new adjacent panels (inventory, settings), follow the same pattern: `setup(menu_open_x, menu_panel_y)`, `_animate_x()`, `is_*_open()`, close interceptor coordination
  - Close interceptor uses `get_tree().create_timer(duration).timeout.connect()` for sequenced animations — watch for edge cases with rapid toggling
  - `close_shop()` is idempotent (checks `_is_open`) — safe to call multiple times
---

## 2026-03-13 - US-022
- Added `discard_refund_ratio: float` export var to FurnitureData (default 0.5)
- Updated all .tres files: table (0.5), sofa (0.5), food_bowl (0.4), toy (0.4), bookshelf (0.6)
- Created inventory_system.gd following CoinSystem pattern: extends Node, signal-driven
- Functions: add_to_inventory(), remove_from_inventory(), get_inventory_count(), get_all_inventory(), set_inventory(), get_inventory_for_save()
- Added InventorySystem node to main.tscn (sibling of CoinSystem)
- Wired into main.gd save/load: inventory persisted under "inventory" key in save_data.json
- Files changed: scripts/furniture_data.gd, scripts/inventory_system.gd (new), scripts/main.gd, scenes/main.tscn, furniture/data/*.tres (all 5)
- **Learnings for future iterations:**
  - inventory_system.gd is referenced as `$InventorySystem` in main.gd — keep node name in sync
  - `remove_from_inventory()` returns bool (like `spend_coins()`) — use for validation before actions
  - Inventory erases keys at 0 count to keep save data clean
  - The shop still uses `_owned` array — future stories (US-023/024) will transition to inventory-based ownership
---

## 2026-03-13 - US-023
- Replaced _owned array system with inventory-based purchasing in shop.gd
- Added quantity selector (-, qty label, +) per shop item row with dynamic total cost display
- Buy button purchases selected quantity, deducts full cost, adds items to inventory_system
- Removed "Owned" state — all items can always be purchased again
- Removed `furniture_purchased` signal — shop now calls inventory_system directly
- Updated main.gd: removed owned_furniture from save data, spawns furniture from furniture_positions keys
- Backward compatibility: legacy owned_furniture entries in save_data.json still handled on load
- Cancel placement now returns item to inventory instead of refunding coins
- Files changed: scripts/shop.gd, scripts/main.gd
- **Learnings for future iterations:**
  - Shop no longer has `get_owned()`, `set_owned()`, `is_owned()`, `remove_owned()` — all ownership is via inventory_system
  - Shop quantities reset to 1 each time the shop is opened
  - `_on_buy()` now takes `(item: FurnitureData, quantity: int)` — quantity captured at rebuild time via `.bind()`
  - Placement mode cancel returns to inventory (not coin refund) — future US-026 will formalize this
  - Furniture spawning on load now uses `_furniture_positions` keys as source of truth for placed furniture
---

## 2026-03-13 - US-024
- Created inventory_panel.gd following the shop.gd adjacent panel pattern
- Inventory panel displays items with icon, name, quantity, Place and Discard buttons
- Place button: removes 1 from inventory, closes panel, enters placement mode via `place_requested` signal
- Discard button: shows ConfirmationDialog with refund amount (coin_cost × discard_refund_ratio), credits coins on confirm
- Items with 0 quantity hidden; empty inventory shows "No items in inventory" message
- Inventory panel rebuilds on `inventory_changed` signal when open
- Added InventoryButton to slide menu between Shop and Edit Layout
- Added `inventory_requested` signal to slide_menu.gd
- Mutual exclusivity: opening Shop closes Inventory and vice versa
- Menu close interceptor handles inventory panel (close panel first, then menu)
- Inventory panel included in passthrough polygon when visible
- Increased slide menu panel_height from 160 to 195 to fit new Inventory button
- Files changed: scripts/inventory_panel.gd (new), scripts/slide_menu.gd, scripts/main.gd, scenes/main.tscn
- **Learnings for future iterations:**
  - inventory_panel.gd uses `get_furniture_data()` via ResourceLoader.exists + load, not from a cached catalog — works but loads on demand
  - `shop_panel.get_furniture_data()` also works for inventory items since the shop loads all .tres files at startup
  - ConfirmationDialog needs `confirmed` and `canceled` signal connections in `_ready()`
  - When adding new adjacent panels, update `_handle_menu_close()`, `_update_passthrough()`, `_on_edit_layout_requested()`, and `_enter_placement_mode()` in main.gd
  - Panel height on slide_menu needs to grow when adding new buttons — update the `panel_height` export var
---
