# Ralph Progress Log
Started: Fri Mar 13 09:16:18 PDT 2026
---

## Codebase Patterns
- Passthrough polygon in main.gd `_update_passthrough()` must use `.abs()` on all Rect2 values before `.merge()` — Godot Rect2 can have negative dimensions from `get_global_rect()`
- GDScript files live in `/workspace/scripts/`, scenes in `/workspace/scenes/`
- No Godot binary is available in this dev environment — manual typecheck verification needed
- Furniture system: FurnitureData resources in `/workspace/resources/furniture/`, Furniture scene at `/workspace/scenes/furniture.tscn`
- Floor Y is computed from `DisplayServer.screen_get_usable_rect()` in main.gd and shared to pet via `pet_sprite.floor_y` — all ground-level logic must use `floor_y` not `screen_get_size().y`
- Slide menu in `scripts/slide_menu.gd` exposes `get_toggle_rect()` and `get_panel_rect()` for passthrough polygon inclusion — future UI panels should follow this pattern
- When replacing UI elements, update both the `.tscn` node tree AND the `@onready` references in the parent script
- To capture all input (disable passthrough), set `get_window().mouse_passthrough_polygon = PackedVector2Array()` and skip `_update_passthrough()` in `_process()`
- Pet can be paused externally via `pet_sprite.paused = true` — `_process()` early-returns, keeping pet frozen in current state
---

## 2026-03-13 - US-012
- Fixed Rect2 negative size error in `_update_passthrough()` by calling `.abs()` on every Rect2 before merging
- Files changed: `scripts/main.gd`
- **Learnings for future iterations:**
  - `Rect2` from `get_global_rect()` can return negative width/height when UI elements are at screen edges
  - `.abs()` normalizes the Rect2 to always have positive dimensions
  - The passthrough polygon is rebuilt every `_process()` frame, so the fix applies continuously
  - No Godot binary in environment — typecheck cannot be run automatically
---

## 2026-03-13 - US-013
- Aligned game floor to taskbar top edge using `DisplayServer.screen_get_usable_rect()`
- Added `_compute_floor_y()` in main.gd that computes floor Y from usable rect with fallback to full screen height
- Pet position, furniture default placement, and coin label all repositioned dynamically to floor_y
- Pet falling logic in pet.gd now lands at `floor_y` instead of `screen_get_size().y`
- Files changed: `scripts/main.gd`, `scripts/pet.gd`
- **Learnings for future iterations:**
  - `DisplayServer.screen_get_usable_rect()` returns a Rect2i excluding OS-reserved areas (taskbar, dock, etc.)
  - Floor Y = `usable.position.y + usable.size.y` — this is the bottom of the usable area (top of taskbar)
  - `floor_y` is shared from main.gd to pet.gd as a public var — future stories should use this instead of raw screen height
  - Pet starting position in the .tscn is overridden at runtime by main.gd `_ready()`, so the scene value doesn't matter
  - Coin label offset_top/offset_bottom are set dynamically — future UI elements near the floor should follow this pattern
---

## 2026-03-13 - US-014
- Implemented collapsible slide-out menu replacing the standalone shop button
- Created `scripts/slide_menu.gd` with Tween-animated panel slide, toggle button at bottom-right
- Menu contains: Shop button, Edit Layout button, separator, and settings placeholder
- Toggle button always visible at bottom-right above taskbar; panel slides left on open
- Clicking outside menu or toggle button again closes it
- Passthrough polygon updated to include toggle button (always) and panel (when open)
- Old standalone ShopButton node removed from main.tscn
- Files changed: `scripts/slide_menu.gd` (new), `scripts/main.gd`, `scenes/main.tscn`
- **Learnings for future iterations:**
  - Slide menu uses `setup(floor_y)` to position itself — must be called after `_compute_floor_y()`
  - `get_toggle_rect()` / `get_panel_rect()` pattern is useful for passthrough — any new UI element that needs passthrough inclusion should expose similar helpers
  - Tween animation: `create_tween()` on the node, kill existing tween before starting new one to avoid conflicts
  - `mouse_filter = MOUSE_FILTER_IGNORE` on the parent Control lets child buttons still receive input while the Control itself doesn't block clicks
  - Manual browser verification needed — no Godot binary available
---

## 2026-03-13 - US-015
- Implemented furniture placement mode: after purchasing furniture, shop closes and a semi-transparent ghost preview follows the mouse cursor constrained to floor Y and screen bounds
- Left-click confirms placement, right-click or Escape cancels and refunds coins
- Passthrough is fully disabled during placement mode so the entire window captures input
- Added `_spawn_furniture_at()` to spawn furniture at a specific position and save immediately
- Added `remove_owned()` method to shop.gd for refund support when placement is cancelled
- Files changed: `scripts/main.gd`, `scripts/shop.gd`
- **Learnings for future iterations:**
  - Placement mode disables passthrough by setting `mouse_passthrough_polygon` to an empty `PackedVector2Array()` — this makes the full window capture all input
  - `_input()` in main.gd handles placement events and marks them as handled to prevent pet/menu from reacting
  - When cancelling placement, must both refund coins via `coin_system.add_coins()` AND remove from owned list via `shop_panel.remove_owned()`
  - `_process()` skips passthrough updates during placement mode to avoid overwriting the disabled state
  - Manual browser verification needed — no Godot binary available
---

## 2026-03-13 - US-016
- Implemented furniture repositioning via edit mode triggered from the slide-out menu's "Edit Layout" button
- Edit mode: highlights all furniture (yellow tint), disables passthrough to capture all input, pauses the pet
- Furniture can be dragged horizontally along the floor (Y stays fixed), clamped to screen bounds
- Overlap detection prevents furniture from being dragged on top of other pieces
- Escape key or clicking "Edit Layout" again exits edit mode
- Positions saved to save_data.json on exit from edit mode
- Added `paused` flag to pet.gd — when true, `_process()` returns early (pet stays IDLE)
- Files changed: `scripts/main.gd`, `scripts/pet.gd`
- **Learnings for future iterations:**
  - Edit mode reuses the same passthrough-disable pattern as placement mode (`PackedVector2Array()` + skip `_update_passthrough()` in `_process()`)
  - Furniture drag uses offset tracking (`_edit_drag_offset_x`) to prevent the piece from snapping to cursor center
  - Overlap detection compares horizontal extents of all furniture pieces — only X matters since all furniture sits at the same floor Y
  - Pet pausing is a simple `paused` bool — `_process()` early-returns, so the pet stays in whatever state it was in (forced to IDLE on enter)
  - `_input()` priority: edit mode is checked first, then placement mode — both return early to prevent other handlers from processing
  - Manual browser verification needed — no Godot binary available
---
